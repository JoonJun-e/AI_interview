일단 a 랑 b 랑 c의 값을 의미로 정해서 설정을 했고요 그리고 가장 최대 값인 max value 같은 경우 max value 라고 입력을 해놓고 시작을 했습니다 그래서 우선 a 값부터 시작해서 b 값과 비교를 하고 그리고 c 값과 비교를 해서 b 값이 클 때 b로 설정하고 c 값이 클 때 c로 설정하는 값을 연상 과정을 비교 연산을 진행하려고 했었는데 이 과정에서 사실 줄바꿈이 갑자기 되지 않아서 계속 진행할 수 없었는데 지금 값대로 간다면 이 부분이 두 번 반복되면서 최종적으로는 c 값이 가장 max value다 라고 결과가 나올 것 같습니다. 가장 3가지 모두 같다면