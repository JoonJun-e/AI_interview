일단 처음에 abc를 정수 형태로 입력을 받고, 이제 value라는 기준 값을 a를 거기다 넣은 다음에, 순차적으로 비교를 해서 만약에 그 값보다 b가 크면 그 값을 b로 바꾸고, 그 값보다 c가 크면 그 값을 c로 바꾸고, 그래서 최종적으로 가장 큰 값을 출력하는 로직입니다. 근데 이제 세 숫자가 모두 같으면 이제 입흐물에서 b나 c가 기준 값보다 크지 않기 때문에, 그냥 출력 값은 처음 값 그대로...