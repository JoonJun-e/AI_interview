일단 a가 max value면이라고 가정을 한 상태에서 풀이 되면 만약에 a라는 값이 들어가겠죠 if 문 다음 걸로 가면 a보다 b가 크면 그냥 그 max value를 b라고 다시 정하게 되는 거고 그 다음 문장이 가서 max value가 c보다 작으면 c가 당연히 제일 큰 값이 돼서 max value가 c로 바꿔 업데이트가 됩니다 그래서 print max value를 하게 되면 실제로 값에 대해서 max value를 알 수 있습니다 세 숫자가 같으면은 여기 시계에서 등식 중에 상립하는 것이 하나도 없기 때문에 max value는 a 값이 나왔는데 어차피 a랑 b랑 c 값은 다 동일하기 때문에 이제 그 값, 그 결과 값의 오류는 없습니다